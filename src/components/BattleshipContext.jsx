import { createContext } from 'react';
import { Battleship } from '../modules/Battleship';

export const BattleshipContext = createContext(null);
export const SetBattleshipContext = createContext(null)